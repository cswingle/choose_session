This directory contains the source files for the CSV used as inputs for
the Python script.  Rather than add scores to every session, I created a
list of unique session names (session_names) added a ranking to that,
and use the R script to produce a CSV combining the sessions with my
scores.

.. vim:ft=rst:fenc=utf-8:tw=72:ts=3:sw=3:sts=3

